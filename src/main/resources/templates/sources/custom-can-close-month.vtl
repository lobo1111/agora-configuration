<?xml version="1.0" encoding="UTF-8" standalone="yes" ?> 
<can-close-month>
    <restrictions>
        ${lol}
        ${restrictions[0]}
        ${restrictions[0].countActivePossessions}
        ${restrictions[0].countActivePossessions()}
        #foreach($restriction in $restrictions)
        <restriction>
            <name>$restriction.getTemplateName()</name>
            <result>$restriction.getResult</result>
            <message>$restriction._message</message>
        </restriction>
        #end
    </restrictions>
</can-close-month>
